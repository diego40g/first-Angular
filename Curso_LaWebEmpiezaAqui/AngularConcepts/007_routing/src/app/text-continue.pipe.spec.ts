import { TextContinuePipe } from './text-continue.pipe';

describe('TextContinuePipe', () => {
  it('create an instance', () => {
    const pipe = new TextContinuePipe();
    expect(pipe).toBeTruthy();
  });
});
